# === Bitbucket Credentials ===

# Base64-encoded Basic Auth token: "username:password" → base64
BITBUCKET_TOKEN=YOUR_BITBUCKET_TOKEN_HERE

# Base URL of your Bitbucket Server (without trailing slash)
BITBUCKET_URL=https://bitbucket.example.com

# === PMD Configuration ===

# Absolute path to the PMD executable
PMD_PATH=/Users/shakenov/Tools/pmd-bin-7.7.0/bin/pmd

# Path to your custom ruleset XML for PMD
PMD_RULESET=/Users/shakenov/ruleset.xml

# Path to suppressions XML file for excluding specific violations
PMD_SUPPRESSIONS=/Users/shakenov/suppressions.xml